# المساعد الشخصي 📱

<div dir="rtl">

## نظرة عامة

**المساعد الشخصي** هو تطبيق Flutter احترافي لإدارة المهام والإنتاجية الشخصية، مصمم بالكامل باللغة العربية مع واجهة مستخدم عصرية وأنيقة.

**المطور:** حيدر فراس  
**الإصدار:** 1.0.0  
**التقنية:** Flutter & Dart

---

## المميزات الرئيسية ✨

### ✅ إدارة المهام
- إنشاء وتعديل المهام اليومية
- 4 مستويات أولوية (منخفضة، متوسطة، عالية، حرجة)
- مهام فرعية لكل مهمة رئيسية
- تواريخ استحقاق وتذكيرات
- تتبع حالة المهام (معلقة، قيد التنفيذ، مكتملة)

### 📝 الملاحظات
- ملاحظات سريعة وسهلة
- تثبيت الملاحظات المهمة
- عرض شبكي جميل
- تصنيف الملاحظات

### 🎨 التخصيص الكامل
- **5 ثيمات مختلفة:**
  - فاتح (Light)
  - داكن (Dark)
  - AMOLED (Pure Black)
  - أزرق (Blue)
  - برتقالي (Orange)
- خط Cairo العربي الجميل
- Material Design 3

### 📊 الإحصائيات
- عدد المهام المعلقة
- عدد المهام المكتملة اليوم
- تتبع الإنتاجية

---

## التقنيات المستخدمة 🛠️

### Core
- **Flutter** - Framework
- **Dart** - لغة البرمجة
- **Provider** - إدارة الحالة

### قاعدة البيانات
- **SQLite (sqflite)** - قاعدة بيانات محلية
- **path_provider** - إدارة المسارات

### UI/UX
- **Google Fonts** - خط Cairo
- **Material Design 3** - نظام التصميم
- **Intl** - التاريخ والوقت

### Other
- **UUID** - معرفات فريدة
- **Shared Preferences** - التخزين المحلي
- **fl_chart** - الرسوم البيانية

---

## هيكل المشروع 📁

```
lib/
├── main.dart                    # نقطة البداية
├── models/                      # النماذج
│   ├── task.dart               # نموذج المهمة
│   ├── subtask.dart            # نموذج المهمة الفرعية
│   ├── note.dart               # نموذج الملاحظة
│   ├── category.dart           # نموذج التصنيف
│   └── goal.dart               # نموذج الهدف
├── providers/                   # إدارة الحالة
│   ├── theme_provider.dart     # إدارة الثيمات
│   ├── task_provider.dart      # إدارة المهام
│   └── note_provider.dart      # إدارة الملاحظات
├── services/                    # الخدمات
│   └── database_service.dart   # خدمة قاعدة البيانات
├── screens/                     # الشاشات
│   ├── splash_screen.dart      # شاشة البداية
│   ├── home_screen.dart        # الشاشة الرئيسية
│   ├── tasks_screen.dart       # شاشة المهام
│   ├── notes_screen.dart       # شاشة الملاحظات
│   └── settings_screen.dart    # شاشة الإعدادات
├── widgets/                     # المكونات
│   ├── task_card.dart          # بطاقة المهمة
│   └── note_card.dart          # بطاقة الملاحظة
└── utils/                       # الأدوات
    └── app_theme.dart          # نظام الثيمات
```

---

## كيفية التشغيل 🚀

### المتطلبات
- Flutter SDK 3.10.3 أو أحدث
- Dart SDK
- Android Studio أو VS Code

### خطوات التشغيل

1. **استنساخ المشروع**
```bash
git clone [repository-url]
cd flutter_application_1
```

2. **تثبيت التبعيات**
```bash
flutter pub get
```

3. **تشغيل التطبيق**
```bash
flutter run
```

### البناء للإنتاج

**Android:**
```bash
flutter build apk --release
```

**iOS:**
```bash
flutter build ios --release
```

---

## قاعدة البيانات 💾

### الجداول (Tables)

#### tasks
- id (TEXT PRIMARY KEY)
- title (TEXT NOT NULL)
- description (TEXT)
- dueDate (TEXT)
- reminderTime (TEXT)
- priority (INTEGER) - 0: منخفضة, 1: متوسطة, 2: عالية, 3: حرجة
- status (INTEGER) - 0: معلقة, 1: قيد التنفيذ, 2: مكتملة
- isCompleted (INTEGER)
- completedAt (TEXT)
- createdAt (TEXT)
- updatedAt (TEXT)
- categoryId (TEXT)

#### subtasks
- id (TEXT PRIMARY KEY)
- taskId (TEXT NOT NULL, FOREIGN KEY)
- title (TEXT NOT NULL)
- isCompleted (INTEGER)

#### notes
- id (TEXT PRIMARY KEY)
- title (TEXT NOT NULL)
- content (TEXT)
- createdAt (TEXT)
- updatedAt (TEXT)
- isPinned (INTEGER)
- categoryId (TEXT)

#### categories
- id (TEXT PRIMARY KEY)
- name (TEXT NOT NULL)
- color (INTEGER)
- icon (INTEGER)

#### goals
- id (TEXT PRIMARY KEY)
- title (TEXT NOT NULL)
- description (TEXT)
- targetDate (TEXT)
- progress (INTEGER) - 0-100
- isCompleted (INTEGER)
- createdAt (TEXT)
- updatedAt (TEXT)

---

## الميزات المستقبلية 🔮

- [ ] شاشات إنشاء/تعديل المهام والملاحظات
- [ ] عرض التقويم للمهام
- [ ] إحصائيات متقدمة مع رسوم بيانية
- [ ] إشعارات وتذكيرات
- [ ] نسخ احتياطي سحابي
- [ ] مزامنة بين الأجهزة
- [ ] Widgets للشاشة الرئيسية
- [ ] إدخال صوتي للملاحظات
- [ ] مشاركة المهام

---

## المساهمة 🤝

نرحب بالمساهمات! إذا كنت ترغب في المساهمة:

1. Fork المشروع
2. أنشئ فرع للميزة (`git checkout -b feature/AmazingFeature`)
3. Commit التغييرات (`git commit -m 'Add some AmazingFeature'`)
4. Push للفرع (`git push origin feature/AmazingFeature`)
5. افتح Pull Request

---

## الترخيص 📄

جميع الحقوق محفوظة © 2024 حيدر فراس

---

## التواصل 📧

للاستفسارات والدعم، يرجى التواصل مع المطور.

---

**صنع بـ ❤️ في العراق**

</div>
